# -*- coding: utf-8 -*-
"""Phase 2 — input processing.

Gather source objects, duplicate them (never touch originals), realize
instances, and convert supported object types to mesh.
"""

_NOT_IMPLEMENTED = "collect.gather is a Phase 2 stub"


def gather(context, props):
    """Return a duplicated, realized, mesh-only working object/collection.

    TODO(phase-2): selection / collection resolution, instance realization,
    curve/text/metaball → mesh conversion, modifier application.
    """
    raise NotImplementedError(_NOT_IMPLEMENTED)
